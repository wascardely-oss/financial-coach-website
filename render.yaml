services:
  - type: web
    name: financial-coach-website
    env: node
    plan: free
    buildCommand: echo 'Build complete'
    startCommand: node app.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
